When present, read `.agents/skills/ace-code-search-mcp/SKILL.md` as the canonical Ace Code Search guidance. Prefer its MCP tools for locating code, symbols, text, files, references, and C/C++ header/source counterparts when `list_indexes` exposes the target workspace. Fall back to `rg`, filesystem search, or direct reads when the MCP server/index is unavailable, `partialIndex` is true and completeness matters, results may be stale, or the target is excluded or unindexed. Do not claim indexed results include unsaved or not-yet-indexed changes.
